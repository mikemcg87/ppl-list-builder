# Synthetic Examples

These files contain fake companies and fake enrichment signals. They exist so the tiering pipeline can be demonstrated without committing scraped prospect data.

Run:

```bash
make demo-tier
```

The command writes `output/sample_prospects_tiered.csv`, which is ignored by Git.
